{
  "name": "web-cam",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "compile": "gcc -shared -fPIC ./compile/intercom.c $(pkg-config --cflags --libs gstreamer-1.0) -lncurses -o ./compile/libintercom.so",
    "test": "gcc ./compile/intercom.c -o intercom     $(pkg-config --cflags --libs gstreamer-1.0)     -lncurses -lpthread -o ./compile/sa_intercom"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "trustedDependencies": [
    "@fresh-editor/fresh-editor"
  ]
}
